assembly-versioning-scheme: MajorMinorPatch
assembly-file-versioning-scheme: MajorMinorPatch
ignore:
  commits-before: 2020-01-01T11:43:45
branches:
  master:
    regex: (origin/)?master
  develop:
    increment: Patch
    tag: develop
    regex: (origin/)?dev(elop)?(ment)?$